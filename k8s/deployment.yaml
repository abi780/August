apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-demo
  namespace: demo-cicd
spec:
  replicas: 3
  selector:
    matchLabels:
      app: flask-demo
  template:
    metadata:
      labels:
        app: flask-demo
    spec:
      containers:
      - name: flask-demo
        image: docker.io/abinaya780/flask-demo:latest
        ports:
        - containerPort: 5000

